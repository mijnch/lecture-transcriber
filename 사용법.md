# Transcriber 사용법

영상이나 음성 파일을 타임스탬프가 붙은 Markdown 전사문으로 바꿔 줍니다.
내 컴퓨터에서만 돌아가므로 무료이고, 파일 용량 제한이 없습니다.

## 쓰는 법

1. 변환할 파일을 `MP4 입력` 폴더에 넣습니다. (하위 폴더에 넣어도 됩니다)
2. **LMS에서 받은 강의노트 PDF가 있으면 `강의자료 PDF` 폴더에 넣습니다.** (권장)
3. `Transcriber 실행.bat`을 더블클릭합니다.
4. 끝나면 `MD 출력` 폴더에서 결과를 확인합니다.

MP4 외에 mp3, m4a, wav, mkv, mov, webm 등 대부분의 형식을 지원합니다.

### 강의노트 PDF를 넣으면

슬라이드 내용을 **화면에서 읽어내는 대신 PDF 원문을 그대로** 싣습니다. 화면 글자는 "지금 몇 쪽인가"를 알아내는 데만 쓰입니다. 그래서

- 글자 깨짐이 사라지고, **표의 열 구분과 빈칸 위치가 보존**됩니다.
- 슬라이드마다 **쪽번호**가 붙습니다.
- PDF의 전문용어를 음성 인식에 미리 알려 주므로 `전환사채`를 `전환사체`로 잘못 듣는 일이 줄어듭니다.

이름이 정확히 같을 필요는 없습니다(`창업과실용법률 12주차.pdf`도 찾아냅니다). 스캔한 그림 PDF는 소용이 없으며, 그때는 실행 중에 알려 드립니다.

## 얼마나 걸리나요

**1시간 영상에 27~43분**입니다 (이 노트북, 기본 설정, 슬라이드 읽기 포함). 같은 9개 강의(318분)를 두 번 돌려 143분과 228분이 나왔습니다 — **같은 작업도 컴퓨터 상태에 따라 1.6배까지 차이가 납니다.** 넉넉히 잡아 계획하세요. 화면이 자주 바뀌는 강의일수록 오래 걸립니다.

**급할 때는 `슬라이드_읽기 = 끔`으로 두세요. 1시간당 13~15분으로 줄어듭니다.** 시간의 대부분은 음성 전사가 아니라 화면 읽기에 쓰입니다(한국어·영어로 두 번 읽고 줄마다 좋은 쪽을 고르기 때문입니다). 다만 슬라이드 내용이 통째로 빠지므로, 강의 이해가 목적이라면 켜 두는 편이 낫습니다.

- 진행률과 남은 시간이 화면에 표시됩니다.
- **처음 한 번은** 음성 인식 모델 약 1.6GB를 내려받습니다. 이때는 진행 표시가 없지만 정상이며, 회선에 따라 10~40분 걸릴 수 있습니다. 그 뒤로는 인터넷 없이 동작합니다.
- 전사 중에는 컴퓨터가 다소 느려집니다. 급한 작업이 있으면 나중에 실행하세요.
- **전원 어댑터를 연결하세요.** 약 15% 빨라지기도 하지만, 그보다 배터리로 오래 돌릴 때 문제가 없도록 하기 위함입니다. 실행 중에는 도구가 절전 진입을 막아두지만, 어댑터가 연결돼 있으면 더 확실합니다.

중간에 멈추려면 **창에서 `Ctrl+C`를 누르세요.** 창을 X로 닫아도 멈추기는 하지만, 그러면 임시 파일 수백 MB가 남습니다(다음 실행이 알아서 치웁니다). 이미 끝난 파일은 남고, 진행 중이던 파일은 다음 실행 때 처음부터 다시 합니다.

## 결과물

문단마다 `[시:분:초]`가 붙습니다. 그 시각으로 영상을 되감으면 해당 대목이 나옵니다.
문단은 문장 중간에서 끊기지 않으므로, 문장을 그대로 검색(Ctrl+F)할 수 있습니다.

**화면의 슬라이드도 함께 읽어 같은 자리에 넣습니다.** 어떤 화면을 띄워놓고 한 말인지 그대로 남습니다.

```
> 🖵 슬라이드 12쪽 [00:26:05 – 00:27:36]
> 국내 벤처캐피탈 경영팀 평가항목
> - CEO의 능력 (예측력, 통찰력, 조직관리능력)
> - CEO의 인성 (신뢰성, 성실성, 도덕성)

**[00:26:06]** 자, 여기 보시면 우선 CEO의 능력을 봅니다...
```

덕분에 말로만 하던 "여기 보시면" 같은 대목의 내용이 채워지고, 슬라이드 제목이 그대로 남아 강의의 목차를 따라갈 수 있습니다.

### 믿을 수 없는 대목에는 표식이 붙습니다

전사문에서 가장 위험한 것은 **그럴듯하지만 틀린 문장**입니다. 그래서 두 가지를 표시합니다.

| 표식 | 뜻 |
|---|---|
| `⚠` | 인식이 흔들린 문단입니다. 원문과 다를 수 있으니 이 문단만으로 사실을 단정하지 마세요. |
| `📺` | 교수의 말이 아니라 **강의 중 재생된 영상**의 자막·말소리입니다. 교수의 주장으로 읽으면 안 됩니다. |

`📺`가 없던 예전 결과물에서는 교수의 설명과 영상 속 인물의 자기소개가 한 문단에 섞여, 읽는 쪽이 그 사람의 경력을 교수의 경력으로 오해할 수 있었습니다.

맨 위에 원본 파일명·길이·언어·읽어낸 슬라이드 수가 적히고, 위 표식이 몇 곳인지도 함께 적힙니다. 녹음 음량이 낮아 상당 부분이 무음으로 처리된 경우에도 경고가 표시됩니다.

## 다시 변환하기

- **설정을 바꾸면** 해당 파일은 자동으로 다시 변환됩니다.
- **원본을 교체하면** (다시 내려받거나 편집) 자동으로 다시 변환됩니다.
- **엔진이 개선되면** 자동으로 다시 변환됩니다.
- **결과 MD를 직접 고쳤다면** 덮어쓰지 않고 건너뜁니다. 필기나 메모가 지워지지 않습니다. 이 보존이 위 세 가지보다 **우선**합니다 — 고친 파일은 설정을 바꿔도 갱신되지 않습니다.
- 그 외에 다시 하고 싶으면 `MD 출력`의 해당 `.md`를 지우고 실행하세요.
- `슬라이드_읽기`를 켜거나 끄면 **음성 전사까지 전부 다시 합니다.** 슬라이드만 추가되는 것이 아니니 시간을 감안하세요.

## 설정 바꾸기 (`설정.ini`)

| 항목 | 기본값 | 설명 |
|---|---|---|
| `우선순위` | `정확도` | `속도`로 바꾸면 약 15% 빨라지지만, 잘못 인식된 구간을 다시 시도하지 않고 타임스탬프도 뭉개집니다 |
| `슬라이드_읽기` | `켬` | 화면의 슬라이드 글자를 함께 싣습니다. **시간이 2~3배가 됩니다** (1시간당 13분 → 27~43분) |
| `슬라이드_언어` | `자동` | 그대로 두세요. 아래 설명 참고 |
| `model` | `large-v3-turbo` | 정확도 최상. `small`은 빠르지만 한국어 정확도가 크게 떨어집니다 |
| `language` | `auto` | 아래 설명 참고 |
| `beam_size` | `1` | 3~5로 올리면 조금 더 정확하지만 느려집니다 |
| `batch_size` | `16` | `우선순위 = 속도`일 때만 쓰입니다 |

### 슬라이드 읽기에 대해

화면이 바뀌는 순간을 찾아 그 슬라이드의 글자를 읽습니다. 화자 얼굴이 한쪽에 나오는 녹화 강의라면 **슬라이드 영역을 자동으로 알아내** 화자를 빼고 읽습니다. 확신이 낮은 줄은 버리지만, 이 걸러내기는 "글자인지"만 보므로 **사진에서 나온 글자 모양 잡음이 남을 수 있습니다.**

- 슬라이드가 없는 영상(얼굴만 나오는 강의, 음성 파일)이면 알아서 건너뜁니다.
- **한국어와 영어를 섞어서 읽지 않고, 각각 읽어 본 뒤 잘 읽힌 쪽을 화면마다 고릅니다.** 섞어서 읽으면 굵은 한글이 영어 낱말로 오인됩니다(`질문을`→`HAS`, `지원내용`→`AMY`). 그래서 `슬라이드_언어 = 자동`을 그대로 두는 것이 가장 정확합니다. 영어 강의에 한국어 슬라이드가 섞여 있어도 읽힙니다.
- 필요 없으면 `슬라이드_읽기 = 끔`으로 두세요. 그만큼 빨라집니다.

알아두실 점:

- **판서와 강조는 읽지 못합니다.** 빨간 펜 동그라미, 형광펜, 굵은 글씨는 글자가 아니라 모양이라 남지 않습니다. 그 문장의 *내용 자체*는 들어가지만 "여기가 중요하다"는 표시는 사라집니다. 교수가 "여기 굵게 해놓은 것"이라고 말한 대목은 무엇을 가리키는지 알 수 없습니다.
- **도표는 원리적으로 옮겨지지 않습니다.** 화살표의 방향, 표의 열 구분, 상자 사이의 관계는 글자가 아니어서 사라지고 낱말만 남습니다. 관계 자체가 내용인 도표(예: 자금 흐름도)는 이 전사문으로 이해할 수 없습니다. 그런 슬라이드는 영상을 직접 보셔야 합니다.
- **빈칸 채우기 강의노트의 빈칸은 빈칸으로 사라집니다.** 정답은 발화 쪽에만 남습니다.
- 표처럼 글자가 빽빽한 슬라이드는 일부 낱말이 잘못 읽힐 수 있고, 행이 누락될 수도 있습니다.

### 언어 설정

`auto`는 **앞부분 30초만 듣고 파일 전체의 언어를 정합니다.** 한국어 강의가 영어 인사말로 시작하면 전체가 영어로 처리되어 정확도가 떨어질 수 있습니다. 결과물 맨 위에 어떤 언어로 처리했는지 적히니 확인해보세요.

- **한 가지 언어만 쓰는 강의**라면 `ko`나 `en`으로 고정하는 편이 정확하고 조금 빠릅니다.
- **한국어와 영어가 섞인 강의**라면 **주로 쓰이는 언어로 두세요.** 소수 언어 쪽으로 고정하면 다수 언어 부분이 크게 망가집니다. (영어 위주 강의를 `ko`로 고정했더니 영어 문장이 엉뚱하게 바뀌는 것을 확인했습니다.)
- 섞인 강의에서는 반대 언어의 고유명사가 소리 나는 대로 적히는 경우가 있습니다(예: `기획의도` → `kihuek eido`). 이는 현재 기술의 한계입니다.

## 문제가 생겼을 때

**이 도구가 이 PC에 의존하는 것**
`engine` 폴더가 함께 있어야 하고, 이 컴퓨터의 Python 3.14에 연결되어 있어 **다른 PC로 복사하면 동작하지 않습니다.** 그 밖에 이 PC에 설치되어 있어야 하는 것이 둘 더 있습니다.

- **ffmpeg** (winget의 `Gyan.FFmpeg`) — 오디오 추출, 화면 캡처, 파일 손상 검사에 씁니다. 없으면 슬라이드 읽기와 손상 검사가 건너뛰어지고, 그때는 실행 중에 경고가 표시됩니다.
- **Tesseract-OCR** (`C:\Program Files\Tesseract-OCR`) — 슬라이드 글자 읽기에 씁니다. 언어 데이터는 `engine\tessdata`에 따로 두었으므로 시스템 설치를 건드리지 않습니다.

**변환에 실패한 파일이 있을 때**
화면의 실패 목록에 이유가 나옵니다. 자주 있는 경우는 이렇습니다.

- *"오디오가 …인데 …까지만 읽혔습니다"* — 파일 복사나 다운로드가 끝나지 않았거나 파일이 손상됐습니다. 완전한 파일로 다시 받으세요. (잘린 채로 전사하지 않고 실패로 처리합니다. 끝에 무음 화면이 붙은 정상 녹화는 실패로 보지 않습니다)
- *"음성이 감지되지 않았습니다"* — 오디오 트랙이 없거나 무음입니다. 다른 파일에서 소리가 나는지 확인하세요.

`실행기록.txt`에 실행 시작·완료·실패·중단이 모두 남으므로, 창을 닫은 뒤에도 무슨 일이 있었는지 확인할 수 있습니다. 슬라이드를 못 읽은 경우도 여기 기록됩니다.

**"같은 이름의 다른 문서가 이미 있어 건너뜁니다"**
`MD 출력`에 이 도구가 만들지 않은 같은 이름의 문서가 있다는 뜻입니다. 그 문서를 지우지 않고 보호한 것이니, 옮기거나 이름을 바꾼 뒤 다시 실행하세요.

**저장 공간을 비우고 싶을 때**

- **원본 MP4는 결과 MD가 나온 뒤 지워도 됩니다.** 원본이 없어도 이미 만든 MD는 그대로 유지됩니다(다시 변환할 계획이 없다면). 강의 파일이 가장 큽니다.
- `engine\models` 안에는 지금 쓰지 않는 모델이 함께 있을 수 있습니다. `설정.ini`의 `model`에 해당하는 폴더만 남기고 나머지 하위 폴더는 지워도 됩니다(모델당 0.5~1.6GB).

## 폴더 구조

```
Transcriber/
├─ MP4 입력/            ← 변환할 파일을 여기에 (하위 폴더 가능)
├─ 강의자료 PDF/         ← LMS에서 받은 강의노트 PDF를 여기에 (선택, 권장)
├─ MD 출력/             ← 결과 Markdown이 여기에
├─ Transcriber 실행.bat  ← 더블클릭으로 실행
├─ 설정.ini             ← 설정
├─ 실행기록.txt          ← 실행·실패 기록
├─ 사용법.md            ← 이 문서
├─ CLAUDE.md            ← 결과물을 Claude에게 읽힐 때의 안내
└─ engine/              ← 내부 엔진 (건드리지 않아도 됩니다)
```
