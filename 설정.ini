; Transcriber 설정 파일
; 값을 바꾼 뒤 저장하고 "Transcriber 실행.bat"을 실행하면,
; 설정이 바뀐 파일은 자동으로 다시 변환됩니다.

[settings]
; 우선순위: 정확도 / 속도
;   정확도 : 인식이 틀렸을 때 스스로 다시 시도하고, 타임스탬프가 정밀합니다 (권장)
;   속도   : 위 검증을 생략해 더 빠릅니다. 대신 잘못 인식된 구간이 그대로 남습니다
우선순위 = 정확도

; 슬라이드 읽기: 켬 / 끔  (다른 말은 인식하지 않습니다)
;   켬 - 화면이 바뀔 때마다 슬라이드의 글자를 읽어 전사문에 함께 싣습니다.
;        말로만 남던 "여기 보시면" 같은 대목의 내용이 채워집니다.
;        1시간 영상 기준 4~8분이 더 걸립니다.
;   이 값을 바꾸면 음성 전사까지 전부 다시 합니다.
슬라이드_읽기 = 켬

; 슬라이드 글자의 언어: 자동 / kor / eng  (언어 코드로 적어야 합니다. "한국어"는 안 됩니다)
;   자동 - 권장. 한국어와 영어로 각각 읽어 본 뒤 화면마다 잘 읽힌 쪽을 고릅니다.
;          kor+eng 처럼 섞으면 굵은 한글이 영어로 오인되므로(질문을→HAS) 섞지 않습니다.
슬라이드_언어 = 자동

; 모델: large-v3-turbo(권장) / medium / small / base
;   small 은 빠르지만 한국어 정확도가 크게 떨어집니다. 영어 전용일 때만 권합니다
model = large-v3-turbo

; 언어: auto(자동 감지) / ko / en / ja / zh ...
;   auto 는 앞부분 30초만 듣고 파일 전체의 언어를 정합니다.
;   한 가지 언어만 쓰는 강의라면 ko 나 en 으로 고정하는 편이 정확합니다.
;   한국어와 영어가 섞인 강의는 더 많이 쓰인 쪽으로 두세요.
language = auto

; 빔 크기: 1 = 가장 빠름(기본), 3~5 = 조금 더 정확하지만 느립니다
beam_size = 1

; 배치 크기: "우선순위 = 속도"일 때만 사용합니다 (2~32, 클수록 빠르고 메모리를 더 씁니다)
;   1을 적으면 의미가 없어 16으로 올립니다.
batch_size = 16
